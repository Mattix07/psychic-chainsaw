# =============================================================================
# EventsMaster - File di Configurazione Ambiente
# =============================================================================
# Questo è un file di esempio. Copia questo file come .env e modifica i valori.
# Il file .env contiene dati sensibili e NON deve essere committato su Git.
#
# Per uso locale:     cp .env.example .env
# Per uso server:     Script setup_server.sh crea automaticamente il file
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_NAME=5cit_eventsMaster
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# -----------------------------------------------------------------------------
# APPLICAZIONE
# -----------------------------------------------------------------------------
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost/eventsMaster

# -----------------------------------------------------------------------------
# EMAIL
# -----------------------------------------------------------------------------
# Configurare con le credenziali del servizio email che si vuole utilizzare
# Esempi comuni: Gmail, SendGrid, Mailgun, SMTP aziendale

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_FROM=noreply@eventsmaster.com
MAIL_FROM_NAME=EventsMaster
MAIL_ENCRYPTION=tls

# Per Gmail, usa una "App Password" invece della password normale:
# https://myaccount.google.com/apppasswords

# -----------------------------------------------------------------------------
# SICUREZZA
# -----------------------------------------------------------------------------
SESSION_LIFETIME=7200

# Chiave segreta per cifratura (genera con: openssl rand -base64 32)
APP_KEY=

# -----------------------------------------------------------------------------
# PAYMENT GATEWAY (per future implementazioni)
# -----------------------------------------------------------------------------
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
PAYPAL_CLIENT_ID=
PAYPAL_SECRET=

# -----------------------------------------------------------------------------
# FILE STORAGE
# -----------------------------------------------------------------------------
# Local storage di default, configurare solo per cloud storage
UPLOAD_DRIVER=local
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET=

# -----------------------------------------------------------------------------
# CACHE & SESSIONS (opzionale)
# -----------------------------------------------------------------------------
CACHE_DRIVER=file
SESSION_DRIVER=file

# Per Redis (opzionale, migliora performance)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_LEVEL=debug
LOG_CHANNEL=daily

# -----------------------------------------------------------------------------
# SVILUPPO
# -----------------------------------------------------------------------------
# Disabilita cache in development
DEV_DISABLE_CACHE=true
